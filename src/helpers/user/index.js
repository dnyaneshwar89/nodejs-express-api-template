// Import all helper functions related to user here
// This way it becomes easy for you to locate functions based on their name of their functionality

const getUser = require('./getUser');

const UserHelper = {
  getUser,
};

module.exports = UserHelper;
